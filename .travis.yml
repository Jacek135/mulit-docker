sudo: required
services:
  - docker

before_install:
  - docker build -t jacekpoczatek/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true jacekpoczatek/react-test npm test

after_success:
  - docker build -t jacekpoczatek/multi-client ./client
  - docker build -t jacekpoczatek/multi-nginx ./nginx
  - docker build -t jacekpoczatek/multi-server ./server
  - docker build -t jacekpoczatek/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push jacekpoczatek/multi-client
  - docker push jacekpoczatek/multi-nginx
  - docker push jacekpoczatek/multi-server
  - docker push jacekpoczatek/multi-worker
